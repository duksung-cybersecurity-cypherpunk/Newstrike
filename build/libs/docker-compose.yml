version: '3'
services:
  springboot-app:
    image: springboot-app
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://192.168.219.221:5432/newstrike
      - SPRING_DATASOURCE_USERNAME=daun
      - SPRING_DATASOURCE_PASSWORD=020620in
      - SPRING_REDIS_HOST=192.168.219.221
      - SPRING_REDIS_PORT=6379
    depends_on:
      - nginx

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
